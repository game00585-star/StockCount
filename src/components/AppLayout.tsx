import {FileSpreadsheet,ClipboardList,PackageCheck,ShieldCheck} from 'lucide-react';import {NavLink,Outlet} from 'react-router-dom';
const links=[['/allowance','ไฟล์ Allowance',FileSpreadsheet],['/movement','ไฟล์รายการเคลื่อนไหว',ClipboardList],['/count','นับสต็อก',PackageCheck]] as const;
export function DesktopSidebar(){return <aside className="hidden min-h-screen w-72 shrink-0 border-r border-rose-100 bg-white p-5 lg:block"><div className="mb-8 flex items-center gap-3 p-2"><span className="grid size-12 place-items-center rounded-2xl bg-rose-700 text-white"><ShieldCheck/></span><div><b className="block text-lg">Audit Stock</b><span className="text-xs text-slate-500">COUNT CONTROL</span></div></div><nav className="space-y-2">{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`nav-link ${isActive?'nav-active':''}`}><Icon size={21}/>{label}</NavLink>)}</nav><p className="mt-10 rounded-2xl bg-rose-50 p-4 text-xs leading-5 text-rose-900">ข้อมูลบันทึกอยู่ในอุปกรณ์นี้ เหมาะสำหรับใช้งาน ณ จุดตรวจนับ</p></aside>}
export function MobileBottomNavigation(){return <nav className="fixed inset-x-0 bottom-0 z-30 grid grid-cols-3 border-t border-rose-100 bg-white/95 px-2 pb-[max(8px,env(safe-area-inset-bottom))] pt-2 backdrop-blur lg:hidden">{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex min-h-14 flex-col items-center justify-center gap-1 rounded-xl text-[11px] font-bold ${isActive?'bg-rose-50 text-rose-700':'text-slate-500'}`}><Icon size={21}/>{label}</NavLink>)}</nav>}
export function AppLayout(){return <div className="min-h-screen lg:flex"><DesktopSidebar/><main className="min-w-0 flex-1 pb-24 lg:pb-0"><Outlet/></main><MobileBottomNavigation/></div>}
