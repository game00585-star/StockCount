import {CheckCircle2,AlertTriangle,FileUp} from 'lucide-react';import type {ParsedMovement,ParsedProduct} from '../types';
export function MatchSummaryCards({items}:{items:{label:string;value:number;tone?:string}[]}){return <div className="grid grid-cols-2 gap-3 md:grid-cols-3 lg:grid-cols-6">{items.map(i=><div key={i.label} className="metric-card"><span className="text-xs text-slate-500">{i.label}</span><b className={`mt-2 block text-2xl ${i.tone||'text-slate-900'}`}>{i.value.toLocaleString('th-TH')}</b></div>)}</div>}
export function FileDrop({onFile,label}:{onFile:(f:File)=>void;label:string}){return <label className="group flex min-h-40 cursor-pointer flex-col items-center justify-center rounded-3xl border-2 border-dashed border-rose-200 bg-white p-6 text-center transition hover:border-rose-500 hover:bg-rose-50"><FileUp className="mb-3 text-rose-700" size={36}/><b>{label}</b><span className="mt-1 text-sm text-slate-500">รองรับ .xlsx และ .xls</span><input className="sr-only" type="file" accept=".xlsx,.xls" onChange={e=>e.target.files?.[0]&&onFile(e.target.files[0])}/></label>}
export function AllowanceImportPreview({rows}:{rows:ParsedProduct[]}){return <div className="table-wrap"><table><thead><tr><th>แถว</th><th>รหัสสินค้า</th><th>ชื่อสินค้า</th><th>หน่วย</th><th>ผลตรวจ</th></tr></thead><tbody>{rows.slice(0,50).map((r,i)=><tr key={i}><td>{r.row}</td><td className="font-mono">{r.productCode}</td><td>{r.productName}</td><td>{r.unit}</td><td><Status ok={r.status==='insert'||r.status==='update'||r.status==='skip'} text={r.status||''}/></td></tr>)}</tbody></table></div>}
function Status({ok,text}:{ok:boolean;text:string}){return <span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-xs font-bold ${ok?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{ok?<CheckCircle2 size={14}/>:<AlertTriangle size={14}/>} {text}</span>}
export function MovementImportPreview({rows,onToggle}:{rows:ParsedMovement[];onToggle:(i:number)=>void}){return <div className="table-wrap"><table><thead><tr><th>เลือก</th><th>รหัสสินค้า</th><th>ชื่อจากไฟล์</th><th>ชื่อจาก Allowance</th><th>หน่วย</th><th>สถานะ</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.productCode}-${i}`}><td><input className="size-5 accent-rose-700" type="checkbox" checked={!!r.selected} disabled={!r.matched} onChange={()=>onToggle(i)}/></td><td className="font-mono">{r.productCode}</td><td>{r.sourceProductName}</td><td>{r.product?.productName||'-'}</td><td>{r.product?.unit||r.sourceUnit}</td><td><Status ok={!!r.matched} text={r.matchReason||''}/></td></tr>)}</tbody></table></div>}
