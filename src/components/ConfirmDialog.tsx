export function ConfirmDialog({open,title,detail,onCancel,onConfirm}:{open:boolean;title:string;detail:string;onCancel:()=>void;onConfirm:()=>void}){if(!open)return null;return <div className="modal-backdrop"><div className="modal-card max-w-sm"><h2 className="text-xl font-black">{title}</h2><p className="mt-2 text-slate-600">{detail}</p><div className="mt-6 grid grid-cols-2 gap-3"><button className="btn-secondary" onClick={onCancel}>ยกเลิก</button><button className="btn-danger" onClick={onConfirm}>ยืนยัน</button></div></div></div>}
